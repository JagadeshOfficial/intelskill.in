spring:
  config:
    import: "optional:file:.env[.properties]"
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Sivani@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        show_sql: false
    show-sql: false
  web:
    resources:
      static-locations: classpath:/static/, file:./uploads/
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:akshjavahub@gmail.com}
    # Do NOT keep a real password in version control. Set MAIL_PASSWORD as an environment variable.
    # The default fallback has been removed to force use of environment variables or a .env loader.
    password: ${MAIL_PASSWORD:tkzmumhstklikcxg}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.lms: DEBUG

jwt:
  secret: mySecretKeyForAdminAuthenticationWithMinimum256Bits
  expiration: 86400000

file:
  upload-dir: ./uploads

# Development toggles
app:
  dev:
    # When true, enables the /api/v1/tutor/debug/last-otp endpoint to retrieve the last generated OTP for an email.
    # Useful for local development when SMTP is not configured. Keep false in production.
    enable-otp-debug: false
